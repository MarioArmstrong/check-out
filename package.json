{
  "name": "check-out",
  "version": "1.0.0",
  "description": "checkOut application with MERN stack",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "vercelBuild": "cd client && npm run build --output build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.22",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.2.7"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}